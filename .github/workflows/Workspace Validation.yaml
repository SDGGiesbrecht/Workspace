# Workspace Validation.yaml
#
# This source file is part of the Workspace open source project.
# Diese Quelldatei ist Teil des qeulloffenen Arbeitsbereich‐Projekt.
# https://github.com/SDGGiesbrecht/Workspace#workspace
#
# Copyright ©2019 Jeremy David Giesbrecht and the Workspace project contributors.
# Urheberrecht ©2019 Jeremy David Giesbrecht und die Mitwirkenden des Arbeitsbereich‐Projekts.
#
# Soli Deo gloria.
#
# Licensed under the Apache Licence, Version 2.0.
# See http://www.apache.org/licenses/LICENSE-2.0 for licence information.

name: Workspace Validation

on: [push, pull_request]

jobs:
  macOS:
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v1
    - name: Refresh
      run: "bash \"./Refresh (macOS).command\""
    - name: Refresh
      run: "bash \"./Validate (macOS).command\" •job macos"
  Linux:
    runs-on: ubuntu-18.04
    steps:
    - uses: actions/checkout@v1
    - name: Refresh
      run: "bash \"./Refresh (macOS).command\""
    - name: Refresh
      run: "bash \"./Validate (macOS).command\" •job linux"
  iOS:
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v1
    - name: Refresh
      run: "bash \"./Refresh (macOS).command\""
    - name: Refresh
      run: "bash \"./Validate (macOS).command\" •job ios"
  tvOS:
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v1
    - name: Refresh
      run: "bash \"./Refresh (macOS).command\""
    - name: Refresh
      run: "bash \"./Validate (macOS).command\" •job tvos"
  Miscellaneous:
    runs-on: ubuntu-18.04
    steps:
    - uses: actions/checkout@v1
    - name: Refresh
      run: "bash \"./Refresh (macOS).command\""
    - name: Refresh
      run: "bash \"./Validate (macOS).command\" •job miscellaneous"
  Deployment:
    runs-on: ubuntu-18.04
    steps:
    - uses: actions/checkout@v1
    - name: Refresh
      run: "bash \"./Refresh (macOS).command\""
    - name: Refresh
      run: "bash \"./Validate (macOS).command\" •job deployment"
