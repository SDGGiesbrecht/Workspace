# Workspace Validation.yaml
#
# This source file is part of the Workspace open source project.
# Diese Quelldatei ist Teil des quelloffenen Arbeitsbereich‐Projekt.
# https://github.com/SDGGiesbrecht/Workspace#workspace
#
# Copyright ©2019–2020 Jeremy David Giesbrecht and the Workspace project contributors.
# Urheberrecht ©2019–2020 Jeremy David Giesbrecht und die Mitwirkenden des Arbeitsbereich‐Projekts.
#
# Soli Deo gloria.
#
# Licensed under the Apache Licence, Version 2.0.
# See http://www.apache.org/licenses/LICENSE-2.0 for licence information.

name: Workspace Validation

on: [push, pull_request]

jobs:
  macOS:
    runs-on: macos-10.15
    steps:
    - name: Check out
      uses: actions/checkout@v1
    - name: Cache Workspace
      uses: actions/cache@v1
      with:
        key: macOS‐${{ hashFiles('.github/workflows/**') }}
        path: .build/SDG/Workspace
    - name: Set Xcode up
      shell: bash
      run: |
        set -x
        export DEVELOPER_DIR=/Applications/Xcode_11.4.app/Contents/Developer
        echo "::set-env name=DEVELOPER_DIR::${DEVELOPER_DIR}"
        xcodebuild -version
        cd /Applications
        ls
    - name: Refresh
      shell: bash
      run: |
        set -x
        find /Applications
        sudo xcode-select -switch /Applications/Xcode_11.4.app/Contents/Developer
        xcodebuild -version
        swift --version
        git clone https://github.com/SDGGiesbrecht/SDGSwift
        cd SDGSwift
        git checkout swift‐5.1
        swift package dump-package
        swift build --verbose
    - name: Validate
      shell: bash
      run: |
        set -x
        swift run workspace validate •job macos •language '🇬🇧EN;🇺🇸EN;🇨🇦EN;🇩🇪DE'
